      public final boolean isInitialized() {
        for (int i = 0; i < getBaseChunkCount(); i++) {
          if (!getBaseChunk(i).isInitialized()) {

            return false;
          }
        }
        return true;
      }

