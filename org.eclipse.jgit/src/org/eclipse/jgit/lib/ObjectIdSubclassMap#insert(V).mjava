	private void insert(final V newValue) {
		int j = newValue.w1 & mask;
		final V[] tbl = table;
		final int end = tbl.length;
		while (tbl[j] != null) {
			if (++j == end)
				j = 0;
		}
		tbl[j] = newValue;
	}

