		/**
		 * @param objectId
		 *            from a "want" line in a fetch request
		 * @return the builder
		 */
		Builder addWantsId(ObjectId objectId) {
			wantsIds.add(objectId);
			return this;
		}

