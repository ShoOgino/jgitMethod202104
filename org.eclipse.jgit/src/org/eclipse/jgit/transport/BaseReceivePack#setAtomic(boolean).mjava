	/**
	 * @param atomic
	 *            true to perform the client's commands as a single atomic
	 *            transaction.
	 * @since 4.4
	 */
	protected void setAtomic(boolean atomic) {
		this.atomic = atomic;
	}

