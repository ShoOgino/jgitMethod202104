		@Override
		public long getLastModified() {
			if (lastModified == 0) {
				try {
					lastModified = fs.lastModified(file);
				} catch (IOException e) {
					lastModified = 0;
				}
			}
			return lastModified;
		}

