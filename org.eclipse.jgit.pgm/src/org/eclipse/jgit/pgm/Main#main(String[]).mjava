	/**
	 * Execute the command line.
	 *
	 * @param argv
	 *            arguments.
	 * @throws Exception
	 */
	public static void main(final String[] argv) throws Exception {
		new Main().run(argv);
	}

