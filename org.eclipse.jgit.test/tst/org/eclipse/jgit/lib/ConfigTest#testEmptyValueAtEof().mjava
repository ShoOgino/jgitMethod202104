	@Test
	public void testEmptyValueAtEof() throws Exception {
		String text = "[a]\nx =";
		Config c = parse(text);
		assertEquals("", c.getString("a", null, "x"));
		c = parse(text + "\n");
		assertEquals("", c.getString("a", null, "x"));
	}

