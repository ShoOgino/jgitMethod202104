	private static boolean same(Cell oldCell, byte[] expVal) {
		if (oldCell == null)
			return expVal == null;

		if (expVal == null)
			return false;

		return Arrays.equals(oldCell.value, expVal);
	}

