	/**
	 * Wrap a database key.
	 *
	 * @param ns
	 *            the namespace the key is contained within.
	 * @param key
	 *            the key to wrap.
	 */
	public CacheKey(Namespace ns, RowKey key) {
		this(ns, key.asBytes());
	}

