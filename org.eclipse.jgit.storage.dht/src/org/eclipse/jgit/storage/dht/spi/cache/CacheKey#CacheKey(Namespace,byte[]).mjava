	/**
	 * Wrap a byte array.
	 *
	 * @param ns
	 *            the namespace the key is contained within.
	 * @param key
	 *            the key to wrap.
	 */
	public CacheKey(Namespace ns, byte[] key) {
		this.ns = ns;
		this.key = key;
	}

