		/**
		 * Initialize a new change.
		 *
		 * @param type
		 * @param key
		 * @param data
		 */
		public Change(Type type, CacheKey key, byte[] data) {
			this.type = type;
			this.key = key;
			this.data = data;
		}

