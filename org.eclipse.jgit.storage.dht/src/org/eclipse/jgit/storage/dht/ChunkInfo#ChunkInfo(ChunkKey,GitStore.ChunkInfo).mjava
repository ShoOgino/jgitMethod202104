	/**
	 * Wrap a ChunkInfo message.
	 *
	 * @param key
	 *            associated chunk key.
	 * @param data
	 *            data.
	 */
	public ChunkInfo(ChunkKey key, GitStore.ChunkInfo data) {
		this.chunkKey = key;
		this.data = data;
	}

