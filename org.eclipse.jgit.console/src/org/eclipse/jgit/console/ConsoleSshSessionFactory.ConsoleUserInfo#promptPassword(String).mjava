		public boolean promptPassword(final String msg) {
			passwd = null;
			char[] p = cons.readPassword("%s: ", msg);
			if (p != null) {
				passwd = new String(p);
				return true;
			}
			return false;
		}

